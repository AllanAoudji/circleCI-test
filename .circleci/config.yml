version: 2.1

jobs:
  test:
    docker:
      - image: cimg/node:18.3.0
    steps:
      - checkout
      - run: node --version
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: 'Say hello'
          command: 'echo Hello, World!'

workflows:
  test-deploy:
    jobs:
      - test
